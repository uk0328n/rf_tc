tr
  td = f.object.advisor.name
  td = f.object.advisor.company_short_name
  td = f.object.advisor.position
  td.row
    .field
      = f.hidden_field :advisor_id
    .field
      = f.hidden_field :event_id
    .field
      = f.select :attendance_type, {"出席" => 1, "欠席" => 2}
